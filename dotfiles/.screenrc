autodetach on
bind .
bind k
bind ^k
bind , next
bind ^h chdir
bind h chdir
bind K kill
bind q only
bind ^q only
bind s split
bind ^s split
bind U utf8
bind x remove
# the next two are inspired by the vim C-w commands
bind = resize =
bind _ resize max
caption always '%{=}%{kg}%-w%52L>%{+b y}%n %t%{-}%+w%{-b g} %-=%{ky}'
hardstatus off
nethack on
startup_message off
vbell on
